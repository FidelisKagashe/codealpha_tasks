{
  "name": "django-socialapp",
  "private": true,
  "scripts": {
    "test": "echo \"This is a Django project. Use: python manage.py test\" && exit 0",
    "build": "echo \"Django project built successfully. Run: python manage.py runserver\" && exit 0"
  }
}
